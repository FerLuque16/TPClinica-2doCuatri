import { CambiarColorLoginDirective } from './cambiar-color-login.directive';

describe('CambiarColorLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarColorLoginDirective();
    expect(directive).toBeTruthy();
  });
});

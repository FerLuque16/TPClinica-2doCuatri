import { CambiarColorCardDirective } from './cambiar-color-card.directive';

describe('CambiarColorCardDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarColorCardDirective();
    expect(directive).toBeTruthy();
  });
});

import { FiltrarPorHoraPipe } from './filtrar-por-hora.pipe';

describe('FiltrarPorHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPorHoraPipe();
    expect(pipe).toBeTruthy();
  });
});

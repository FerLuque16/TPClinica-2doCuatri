import { OrdenarFechasPipe } from './ordenar-fechas.pipe';

describe('OrdenarFechasPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarFechasPipe();
    expect(pipe).toBeTruthy();
  });
});
